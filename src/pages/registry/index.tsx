import React, { useCallback } from "react";
import { View, Text, Button, Image } from "@tarojs/components";

import './index.less'

const Registry = () => {
  return (
    <View className="registry">
      Registry
    </View>
  );
};

export default Registry;
