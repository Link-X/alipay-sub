import React, { useCallback } from "react";
import { View, Text, Button, Image } from "@tarojs/components";
import { AtButton } from "taro-ui";

const Index = () => {
  return (
    <View className="wrapper">
      <AtButton>分包1</AtButton>
    </View>
  );
};

export default Index;
